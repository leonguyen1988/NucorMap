using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace NucorMap.App_Start
{
    public static void BundleConfig(BundleColeection bundles)
    {
        // create an object of ScriptBundle and
        // specify bundle name (as virtual path) as contructor parameter
        bundles.Add
    }
}